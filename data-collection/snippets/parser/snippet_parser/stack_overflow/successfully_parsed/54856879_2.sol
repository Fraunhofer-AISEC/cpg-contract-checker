uint value = 5;
uint tempValue = value * 10;
uint lower_threshold = tempValue / 2;
uint upper_threshold = tempValue * 3 / 2;

if(tempValue >= lower_threshold && tempValue <= lower_threshold) {
    
}
