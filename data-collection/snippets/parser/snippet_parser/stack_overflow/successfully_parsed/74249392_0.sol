struct Player {
    string name; 
    uint gameId; 
    uint buyinAmount; 
    uint withdrawalAmount; 
    bool withdrawalReq; 
    bool verifyReqs; 
    bool hasWithdrawn; 
    bool isInGame; 
    bool isHost; 
}
