    public returns (uint256) {

    uint256 output = myFunc_Process_Step1(rAmount, rTransferAmount, rFee, tTransferAmount, tFee, tLiquidity, tCharity, tmarketing);

    return output;

}

    function myFunc_Process_Step1(uint256 rAmount, uint256 rTransferAmount, uint256 rFee, uint256 tTransferAmount, uint256 tFee, uint256 tLiquidity, uint256 tCharity, uint256 tmarketing) public pure returns (uint256) {

    uint256 local1 = 1;

    uint256 local2 = 2;
    
    uint256 local3 = 3;
    
    uint256 local4 = 4;
    
    uint256 local5 = 5;
    
    uint256 local6 = 6;
    
    uint256 local7 = 7;
    
    uint256 local8 = 8;

    return rAmount + rTransferAmount + rFee + tTransferAmount + tFee + tLiquidity + tCharity + tmarketing; 

}
