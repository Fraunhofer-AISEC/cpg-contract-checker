contract token {
    uint owner;
    function foo(uint fooval) bar(fooval) view public{
    
    

        uint infoo;
        infoo = owner;
    }

    modifier bar(uint barval) {
        uint hold;
        if (barval > 100) {
           
           hold = owner; 
        }
        _;
    }
}
