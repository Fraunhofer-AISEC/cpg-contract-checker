mapping(address => bool) didSomething;
address[] currentAddresses;
uint lastAddress;

function Next () public returns (address) {
if (lastAddress == 1) {
   currentAddresses[2];
   lastAddress+1;
                      if (didSomething[currentAddress] = true) {
                                  continue;
                               }
                      }

returns (currentAddresses[2]);
