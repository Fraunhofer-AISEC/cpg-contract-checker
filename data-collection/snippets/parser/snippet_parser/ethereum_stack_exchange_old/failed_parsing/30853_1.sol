contract Foo {

    uint[] public foo;  

    function funcA() public returns (uint[]) { 
        uint a[];
        
        return a
    }

    function funcB() public returns (uint a[]) {
        
        return a
    }
}
