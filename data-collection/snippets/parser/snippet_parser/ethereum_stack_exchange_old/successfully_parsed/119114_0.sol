function cells() public {
        uint256 canvas = 4000;
        uint256 c = 1;
        uint256 columns = 4;

        uint256 x = 400000000000000000 * (100000000000000 / 400000000000000);
        uint256 _x = canvas * (1 / 4 * (10**14));
}
