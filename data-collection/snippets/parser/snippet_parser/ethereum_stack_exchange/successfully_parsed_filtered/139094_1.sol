  address payable not_called;
  function bug() external payable { 
          require(bool);    
          if( ! (msg.sender.call() ) ){ 
              return ;
          }
          not_called = true; 
      }
