
pragma solidity ^0.8.0;

import "./constants.sol";

contract Foo {
    
    

    
    function myFunction() external pure returns (uint256) {
        return MY_CONSTANT; 
    }
}

contract Bar {

    
    function myFunction() external pure returns (uint256) {
        return MY_CONSTANT; 
    }
}
