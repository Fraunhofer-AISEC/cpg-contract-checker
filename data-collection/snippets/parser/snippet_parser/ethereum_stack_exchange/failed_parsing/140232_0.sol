contract Timelock {
mapping(address => uint) balance;
mapping(address => uint) timelock;

function(uint string _time 
  balance[msg.sender] += msg.value;
  timelock[meg.sender] = block.timestamp + _time

}

}

