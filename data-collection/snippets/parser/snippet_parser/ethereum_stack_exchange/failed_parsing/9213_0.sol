modifier send_it(uint _amount, address _address ) {
    if (msg.value < _amount)
        throw;
    _
    if (msg.value > _amount)
        _address .send(_amount - msg.value);
}

  function x(address _newOwner)
        send_it(200 ether,msg.sender) 
    {
     
     }
