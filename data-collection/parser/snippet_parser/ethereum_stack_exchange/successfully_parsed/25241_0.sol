function getQuotient() returns (uint256) {

    uint256 numMembers = uint256(someStruct.someArray.length);
    uint256 sampleSum = someSameplSum; 
    uint256 avgNum = sampleSum / numMembers;

    return avgNum;
}
