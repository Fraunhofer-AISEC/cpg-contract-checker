contract c{
    enum Numbers {zero,one,two,three,four}

    Numbers number;
    function checkIfExist(string member) returns(bool exists){

         

         for(uint i=0;i<Number.length;i++){
            if(string(Number[i]==member){ 
                return true;
            }
            else{
                exists=false;
            }
         }

         
         uint flag=Number.member;
         if(flag>0){
              return true;
         }
    }
}
