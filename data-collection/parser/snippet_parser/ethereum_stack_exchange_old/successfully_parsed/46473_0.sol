mapping (uint256=>string)  fieldNameArr; 
function getAllFields()  public view returns (string){
    uint g;
    for(g=0;g<=e ;g++){

        arr.push(bytes(fieldNameArr[g]));
        arr.push(",");

    }  
return (arr);
} 
