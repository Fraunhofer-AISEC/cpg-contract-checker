function mix() public returns (bytes) {  

    bytes first;
    for (uint8 cnt = 0; cnt<8; cnt++) {
        first.push(9)
    }


    bytes second;
    for (uint8 cnt2 = 0; cnt2<8; cnt2++) {
        second.push(8);
    }

    return first;
}
