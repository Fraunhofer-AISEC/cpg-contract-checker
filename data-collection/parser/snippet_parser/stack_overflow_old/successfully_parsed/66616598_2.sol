pragma solidity ^0.7.0;

contract Test {
   function divide() public pure returns (uint) {
        return uint(7) / uint(2); 
   }
   
   function add() public pure returns (uint) {
        return 3.5 + 0.5; 
   }
}
