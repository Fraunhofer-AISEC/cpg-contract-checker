    (,uint256 alpha) = testFunction(); 
    (uint256 beta,) = testFunction(); 
            function testFunction() public pure returns(uint256,uint256){
                    return (1,2);
            }
