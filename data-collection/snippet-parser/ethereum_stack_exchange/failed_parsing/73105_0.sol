function addressesToBytes(address add1, address add2) public view returns(bytes combined) {
    combined = new bytes(40); 
    
}

function bytesToAddresses(bytes combined) returnes(address add1, address add2) {
     
}
